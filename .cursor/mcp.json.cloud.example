{
  "mcpServers": {
    "rental-snowball": {
      "command": "npm",
      "args": ["run", "mcp:stdio"],
      "env": {
        "PORTFOLIO_TRACKER_URL": "${env:PORTFOLIO_TRACKER_URL}",
        "PORTFOLIO_API_KEY": "${env:PORTFOLIO_API_KEY}"
      }
    }
  }
}
